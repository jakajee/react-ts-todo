import React from 'react';
import MainRoute from '../route';
import Root from '../root';

const App = (props: any) => {

    return (
        <Root>
            <MainRoute />
        </Root>
    )
}

export default App;